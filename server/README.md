Manual virtual environment creation is not necessary, Poetry will handle that.

Install dependencies

```sh
poetry install
```

Seed the database

```sh
poetry run seed
```

Run in dev mode

```sh
poetry run dev
```
